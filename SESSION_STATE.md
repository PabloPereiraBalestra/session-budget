# SESSION_STATE

Backlog source: IMPROVEMENTS.md §Accepted (triage confirmed by the user on 2026-07-05, via AskUserQuestion in session B13).

## Pending blocks
<!-- ordered by dependency: [TAG] size est_points — description | dep -->
(empty)

## In progress
(none)

## Completed
- [MECHANICAL] S — packaged as a personal skill at `~/.claude/skills/session-budget/` (`SKILL.md` + `references/SPEC.md`, copied byte-for-byte from this repo — same precedent as B7 with repo-trust), at the user's request ("how do I use this in other projects"). §8 of SPEC.md already had it designed ("after 2-3 validated sessions" — already well past that). Did not come from the backlog (backlog was empty) — deviation noted, same as B10. | commit null (deliverable outside any working tree, see note in Pending validation) | session % 7→33 | actual 26 points (no prior estimate, ad-hoc block; delta includes several accumulated conversation turns, not just the file copy)
- [DESIGN] S — spec v25 (2026-07-06, approved by the user): root-cause fix for the S/Sonnet estimation-error flag — "Budget tracking" now requires using the calibrated median of the (size, model) bucket when assigning a block's estimate, not the raw default, once the bucket has n≥3. B19-B21/B24-B25 had used a fixed est=5 while the current median had already been 3→4 since before B19; that, not the S size itself, explained 80% of the error. Does not redefine what S/M/L mean. CLAUDE.md resynced, verified byte-for-byte. | commit 891d46f | session % 3→7 | actual 4 points
- [MECHANICAL] S — B1: scaffold session-budget (SKILL.md, references/SPEC.md, README.md, LICENSE, .gitignore) | commit 6e7a450 | session % 57→58 | actual 1 point
- [MECHANICAL] S — B2: git init (main), first commit, gh repo create --public, push, resync references/SPEC.md to v13 (this repo becomes canonical, Downloads is no longer read as the source) | commit 6e7a450 | session % 58→9 (spans_reset: the 5h window reset during the block, actual not valid) | actual null
- [MECHANICAL] M — B3: CLAUDE.md (protocol, identical to verified §1.2), SESSION_STATE.md, budget_log.jsonl, .claude/agents/implementer.md; §4 tests 6/7/8 green | commit af48c95 | session % 9→10 | actual 1 point
- [DESIGN] S — B4a: preflight §0 + table §0.3 of REPO_TRUST_SPEC on repo-trust — done in the repo-trust repo (another session), reconciled here on 2026-07-04. No file changes (research/confirmation). | commit n/a (repo-trust repo) | session % n/a (manual mode in that session) | actual null
- [MECHANICAL] M — B4b: deliverables §1 of the trust spec (security.yml, scorecard.yml, dependabot.yml, SECURITY.md, badges README) + local §4 tests 1-7, all green — done in the repo-trust repo. | commit e3aeb56 (repo-trust repo) | session % n/a (manual mode in that session) | actual null
- [MECHANICAL] S — B5: push + post-push §4-8 tests (Security tab with SARIF) and §4-9 (Scorecard published, badge 200, score 5.8/10) — done in the repo-trust repo. | commit decd340 (repo-trust repo) | session % 32→39 | actual 7 points
- [MECHANICAL] S — B6: trial release v0.1.0 + §4-10 test (sbom.cdx.json attached, parses as valid CycloneDX) — done in the repo-trust repo. | commit n/a (release + generated asset, repo-trust repo) | session % 41→42 | actual 1 point
- [DESIGN] S — B8: `budget-auditor` subagent contract drafted and confirmed by the user. Personal scope location (`~/.claude/agents/`) verified against official subagent docs, not assumed; project>user precedence confirmed. Constraints a-e all satisfied (distinctive name, model:sonnet, 3-source contract, structural independence via the subagent's own context window, on-demand trigger with no auto-trigger on session_end). | commit n/a (design, no file changes) | session % n/a (manual mode) | actual null
- [MECHANICAL] S — B9: `~/.claude/agents/budget-auditor.md` installed (created from scratch, didn't exist); spec bumped to v14 (§0.1 table, new §1.6, §4-10 test, §5 Budget report prompt updated). Delegated to implementer, diff verified by the orchestrator. | commit 84c0e0d | session % n/a (manual mode) | actual null
- [MECHANICAL] S — B10: PR #2 ultrareview fixes applied, spec bumped to v15: (1) budget-auditor §1.6 template's YAML frontmatter fixed (illegal `: ` in a plain scalar, verified with PyYAML) and installed corrected at `~/.claude/agents/` locally (§4-10 test green); (2) `"parallel":true` setter rule added to §1.2 Metrics logging + CLAUDE.md resynced; (3) correction line added to budget_log.jsonl for B8's commit:null; (4) §7 Scope now references §1 instead of enumerating it. Run in the main thread (came out of the review conversation, not the planned backlog) — deviation from routing to implementer, noted. | commit 0e3ea1d | session % 42→45 | actual 3 points
- [MECHANICAL] S — B7: repo-trust packaged as a personal skill at `~/.claude/skills/repo-trust/` (SKILL.md + references/SPEC.md), copied byte-for-byte from `C:\Users\pablo\OneDrive\Documentos\GitHub\repo-trust\`. Delegated to implementer; orchestrator independently verified with `diff` + confirmed both repos (session-budget, repo-trust) had no git changes. Skill already visible in the session's active skill list. | commit null (deliverable outside any working tree — see Backlog gap above) | session % 47→52 | actual 5 points
- [MECHANICAL] S — B11: finding from the first formal `budget-auditor` audit (invoked on demand, blind, over budget_log.jsonl + SESSION_STATE.md + range 6e7a450..d16d615): CLAUDE.md had gone out of sync with §1.2 v16 (commit aa15795 fixed the §2/§5 references only in references/SPEC.md, not in the installed CLAUDE.md). Resynced; verified with post-edit grep. Also fixes the stale "v15" mention in Minimal context to resume. | commit 0dee485 | session % 2→2 (post-reset, resets_at changed from 1783222800 to 1783240800 between B7 and B11 — new window) | actual 0 points
- [DESIGN] S — B12: spec bumped to v17, approved by the user after reviewing the proposed text. Closes the 3 schema gaps found by the audit (§1.2 Metrics logging): `commit:null` outside manual mode now requires a `note` field and forbids reusing another block's commit (removes the B8/B9 pattern); `correction.set` can no longer be empty, new `note` line type for annotations with no value change (removes the B7 pattern); `"ts_approximate":true` for timestamps shared between blocks closed in the same wrap-up. Also closes §0.1: a version bump must resync CLAUDE.md in the same commit or explicitly note if it couldn't (the exact gap left by aa15795, already resolved in B11). CLAUDE.md resynced and verified byte-for-byte against §1.2 via script. Not retroactive — historical budget_log.jsonl lines stay as they are. | commit 15e13e1 | session % 11→14 | actual 3 points
- [DESIGN] S — B13: full brainstorm + triage of IMPROVEMENTS.md. 5 new original ideas added (weekly pacing, usage history, reset-aware no-go, burning expiring margin, calibration guardrails); 9 triaged with the user (4 questions, all resolved with the recommended option): 5 accepted with design resolved — 4 proposals merged into the unified allowances monitor —, 1 discarded (history) with reopening condition. Backlog B14-B17 planned. Est 5, actual 16: 3x overrun, no mid-block warning because the block ran in few long turns and the snapshot only updates between turns — evidence for the S/Fable bucket (brainstorming DESIGN on Fable high-effort costs ~3x the S default). | commit 2652889 | session % 22→38 | actual 16 points

- [DESIGN] S — B14: spec bumped to v18, text approved by the user before the commit. Calibration guardrails in Self-tuning §1.2: a bucket needs ≥3 qualifying actuals to override the lower fallback level, actuals=0 always count, parallel/spans_reset remain the only valid exclusions (removes B11's discretionary-exclusion pattern). CLAUDE.md resynced in the same commit, verified byte-for-byte by script (47 identical lines between markers). | commit c5e5c7f | session % 47→63 | actual 16 points

- [DESIGN] M — B15: spec bumped to v19, text approved by the user before the commit. Unified allowances monitor ("Allowance pacing" subsection in §1.2: linear used-vs-elapsed projection per cycle, waste alert ≥70%/<60%, seven_day wall alert, check only on resume/checkpoints) + §1.7 ~/.claude/allowances.json (created install-if-absent, §4-11 test green, null fields until the user declares cycle date and runs used) + reset-aware no-go (<30 min) + "effort" field in the block-line schema + staleness fix (later write from another live window = fresh + parallel). CLAUDE.md resynced, verified byte-for-byte (55 lines). Logged parallel:true — repo-trust session active in parallel throughout the block. | commit 0882c85 | session % 6→23 | actual 17 points (doesn't feed calibration due to parallel)

- [DESIGN] S — B16: spec bumped to v20, text approved by the user before the commit. New §5.1: prescriptive report templates (block checkpoint, session close, resume) with ✅⚠️🛑🔄📉📈⏸️ emoji legend, + binding line in §1.2 Work block protocol. CLAUDE.md resynced, verified byte-for-byte (56 lines). Before the block, admin: parallel corrections for B13/B14 (ac9355d), account reading via Chrome (usage page) and filling ~/.claude/allowances.json with evidenced data (ultrareview 1/3, resets 2026-08-01). | commit d2e87f9 | session % 35→36 | actual 1 point

- [DESIGN] S — B17: spec bumped to v21, text approved by the user before the commit. New "Parallel lanes (conditioned)" subsection in Work block protocol: batches of independent [MECHANICAL] blocks (completed deps, disjoint file scope) can run in parallel via separate implementer invocations, gated by the SUM of the batch's estimates (not the max) + buffer, each log line flagged "parallel":true. Hard-gated at ≥10 qualifying non-null actuals — currently 7, the feature is written but dormant by design (same threshold as Self-tuning's second-order rules). Last block of the B14-B17 backlog. CLAUDE.md resynced, verified byte-for-byte (62 lines). | commit cd82142 | session % 36→39 | actual 3 points
- [DESIGN] S — B18: spec bumped to v22, text approved by the user before the commit. ≥25% elapsed-cycle guard added to the wall alert (seven_day) in Allowance pacing — closes the false positive detected in B15-B16 dogfooding (12% elapsed already projected >100%). Lower threshold than the waste alert's 70%: acting early on wall risk pays off more than acting late. CLAUDE.md resynced, verified byte-for-byte (62 lines). | commit d0fbf32 | session % 39→2 (spans_reset: the 5h window reset during the block) | actual null

- [DESIGN] S — B25 addendum: the user asked to include the bump B25 had left proposed but uncommitted. Spec bumped to v24 (2026-07-06): `agent_model` field in the Metrics logging schema (records which model executed a delegated block's subagent when it differs from the main thread; observability metadata, does NOT enter the (size, model) calibration keys). CLAUDE.md resynced in the same commit, verified byte-for-byte (section between markers identical to §1.2). Did not come from the planned backlog (B25 had already closed) — deviation noted, same as B10. The 5h window reset while this block was being built (resets_at changed), so the actual is not valid. | commit 75e6c88 | session % 44→3 (**spans_reset:true**) | actual null
- [DESIGN] S — B20: plan for blocks B21-B25 for the Fable-fee experiment + design of the portfolio token allocator (full backlog, IMPROVEMENTS.md §Accepted). | commit a1e12c9 | session % 10→19 | actual 9 points
- [DESIGN] S — B25: findings report in `references/FABLE_FEE_FINDINGS.md`. **The "Fable lane" hypothesis was NOT confirmed** — opposite direction from the original observation: Fable cost ~4x more five_hour per token than Sonnet in the B23/B24 comparison (+11pts/62k tokens vs +4pts/90k tokens), even though weekly Fable did move only with the Fable agent (+1pt vs +0 for the Sonnet control). Simpler explanation: the original note probably confused which pool had moved little. IMPROVEMENTS.md updated: "Fable lane" moves to Discarded with evidence and reopening condition; the measurement and the allocator move to Shipped. **No spec bump was committed** — the policy that would have motivated it has no support; the `agent_model` field (useful independently, already used ad-hoc in B23/B24) is left proposed for explicit user approval, not yet applied (hard protocol rule: structural changes only with approval). | commit 810dadd | session % 43→44 | actual 1 point
- [MECHANICAL] S — B24: allocator tests + docs, control run delegated to a Sonnet agent (implementer) — 31/31 own tests pass (lightweight PS runner, only Pester v3.4 inbox available, not worth fighting its old syntax), 0 bugs found in assign.ps1 (all 7 behaviors from DESIGN.md matched on the first try), README expanded with a Testing section. Agent finding: `C:\Users\pablo` is its own git repo (toplevel), empty (0 commits) — left untouched, not the intended destination of `~/.claude/portfolio/`, stays as an observation for the user, not a correction of B22/B23. **Experiment control**: five_hour 39%→43% (+4pts) despite MORE work than B23 (90673 tokens/37 tool_uses vs 62308/14) — less than half of B23's delta. Fable weekly unchanged (24%→24%, correct, Sonnet doesn't touch it). Full interpretation in B25. | commit null (deliverable outside any working tree, see note in Pending validation) | session % 39→43 | actual 4 points (est 5)
- [MECHANICAL] M — B23: allocator implemented by a Fable agent (subagent_tokens=62308, 14 tool_uses) at `~/.claude/portfolio/{assign.ps1,projects.json,README.md}` (outside the repo). Deviation reported by the agent: the block regex was widened from `B\d+` to `[A-Z]+\d+` because repo-trust uses `I3/I4/I5/R1` IDs, not `Bxx` — a real finding, avoids misclassifying that project as an empty backlog. Smoke test against real files: recommends session-budget (B24, MECHANICAL S 5, sonnet — no fresh fable_weekly data in allowances.json) and repo-trust as an alternative. **Central experiment finding**: five_hour went from 28%→39% (+11pts) during the delegation — almost the same as a normal M block would have cost in the main thread, and very different from the original observation (~200k tokens moving the pool only 1pt). Fable weekly and all-models weekly only +1pt each. Parallel contamination during the interval can't be fully ruled out (only session_id was checked at the endpoints, not continuously) — to be resolved in B25. Also fixed a bug of my own: my B22 edit had eaten the leading dash of this same line in SESSION_STATE.md (visible when the agent read it, already fixed). | commit null (deliverable outside any working tree, see note in Pending validation) | session % 28→39 | actual 11 points (est 12)
- [DESIGN] M — B22: design of the portfolio token allocator at `~/.claude/portfolio/DESIGN.md` (outside any repo, same precedent as B7). Resolves the 4 open items: (1) `~/.claude/portfolio/{projects.json,assign.ps1,last_recommendation.json}`; (2) allowances.json extended with a `fable_weekly` block populated on-demand via Chrome, null-until-declared same as `ultrareview`; (3) effort passed through from the last logged value, no recalculation; (4) simple integer priority in projects.json. Algorithm: urgency=priority+deadline_boost, ranking, regex scoped to the fixed `## Pending blocks` format to read each project's next block, recommends Fable only if the block is MECHANICAL and fable_weekly has fresh margin (<24h), never for DESIGN. Pure advisor — doesn't run go/no-go or trigger agents. Design done without an explicit user approval pause (autonomous B21-B25 run authorized), decisions documented for later review if adjustment is needed. | commit null (deliverable outside any working tree, see note in Pending validation) | session % 26→27 (**parallel:true** — a `usage_snapshot.json` write with another live window's session_id was detected, initium project, during this block) | actual 1 point (doesn't feed calibration)
- [MECHANICAL] S — B21: `scripts/capture_meters.ps1` (reads usage_snapshot.json, receives Chrome meters as parameters, appends to new `fable_fee_log.jsonl` at repo root). Baseline recorded: snapshot 5h=25%/7d=23%, Chrome session=26%/all-models weekly=24%/Fable weekly=23% (Sat 7:59PM, matches the snapshot's seven_day.resets_at). Run without a budget re-gate between blocks (explicitly authorized by the user to run B21-B25 back to back, Fable-fee experiment). | commit f97f7f4 | session % 25→26 | actual 1 point
- [DESIGN] S — B19: spec bumped to v23, text approved by the user before the commit (proposed by budget-auditor). Self-tuning §1.2 now requires explicitly reporting on every resume whether second-order rules are active (≥10 threshold) and evaluating the 3 conditions even if none fire; SESSION_STATE.md's Cost calibration becomes the single source of medians, any other recap must reference it. Motivated by a blind audit over 15e13e1..HEAD (post-B18) that found the threshold already crossed without evaluation and a parallel recap out of sync. CLAUDE.md resynced, verified byte-for-byte (62 lines). | commit 756bb05 | session % 2→10 | actual 8 points

## Cost calibration
<!-- medians by (size, model) from budget_log.jsonl, e.g. S/Sonnet=4, M/Sonnet=9, M/Fable=14 -->
<!-- defaults when no data: S=5 M=12 L=25 | current buffer=10 cap=20 -->
14 qualifying actuals (B1=1, B3=1, B7=5, B10=3, B11=0, B12=3, B16=1, B17=3, B19=8, B20=9, B21=1, B23=11, B24=4, B25=1; excluded B2/B18/spec-bump-v24 spans_reset, B13/B14/B15/B22 parallel). v18 rule (n≥3): **S/Sonnet=4** (n=10, last 5: 8,9,1,4,1 → median 4 — up from 3, the bucket now has its own series, no more needed for the n≥3 gate). S/Fable n=2 (3,1) <3 → falls through to **size-only median S=4** (n=12, all S entries, last 5: 8,9,1,4,1 — matches S/Sonnet because the 5 most recent S blocks were all Sonnet). **1 more own (non-parallel) S/Fable actual needed for that bucket to stop inheriting from size-only.** M/Sonnet n=2 (1, 11) <3 → **default M=12** (size-only M also n=2, same two points, same result). **1 more M actual (any model) needed to leave the default.** No L data → **default L=25**, 3 more needed. Buffer=10, cap=20 (no second-order changes: 0 limit_hit, 0 budget_gate closes in the last 3 sessions).

**Second-order rules ACTIVE** (the log has 18 block lines with non-null actual, well above the threshold of 10 — count includes lines flagged parallel/spans_reset per the B18/B19 audit correction):
- limit_hit streak (last 3 sessions): does not apply, 0 limit_hit in the whole log.
- budget_gate streak (last 3 sessions, end_pct<75): does not apply, 0 budget_gate closes in the whole log (all user_cut or work_done).
- Median error >40% over the last 5 actuals per bucket: **S/Sonnet=80%** 🚩 (n=10) — **S/Fable=60%** 🚩 (n=2, small data) — **M/Sonnet=50%** 🚩 (n=2, small data). **Resolved 2026-07-06 (spec v25, approved by the user)**: the root cause of S/Sonnet wasn't that the S size was miscalibrated — it was that B19-B21/B24-B25 were estimated with the fixed default (est=5) instead of the calibrated median that was already current at the time (3, then 4; n≥3 since before B19). Spec bump: "Budget tracking" now requires using the calibrated median of the (size, model) bucket when assigning a new block's estimate — even re-estimating backlog already written in SESSION_STATE.md at the next resume — and reserving the raw default only for buckets without n≥3 yet. Did not redefine what S/M/L mean (still silently forbidden) — fixed that estimation wasn't applying the already-available calibration. From here on, every new S/Sonnet block should be estimated at ~4 (the current median), not 5; the error should drop with future data without needing to touch anything else. S/Fable and M/Sonnet remain n<3 — they'll be re-evaluated on their own once they cross the threshold.

## Pending validation
- First formal `budget-auditor` audit run 2026-07-05 (blind, over budget_log.jsonl + SESSION_STATE.md + git range 6e7a450..d16d615). Result: checks 2, 3, 4a, 4d pass; check 1 (schema) fail due to the gaps now closed in B12; check 4c informational (calibration phase, doesn't formally apply yet); 4b n/a (no budget_gate cuts yet). Actions taken: B11 (CLAUDE.md resync) + B12 (spec v17, closes the schema gaps). Full subagent report not persisted to a file — re-invoke if needed again.
- Auditor scope note: B4a/B4b/B5/B6 (credited to repo-trust repo commits) are correct but not verifiable from *this* project's sources — they live in repo-trust's budget_log.jsonl, outside the range audited here. Not a protocol violation, a known limitation of auditing one project at a time.

## Session close (2026-07-05)
- Session closed by work_done (empty backlog), auto mode, 4 blocks (B10, B7, B11, B12), 5h at 16% at close. Repo clean and pushed (HEAD 7c0a3bb + this close commit).
- Next session: start with the Resume prompt (§5 of the spec). No pending blocks or open spec gaps — B7-B12 closed everything in flight. Only non-actionable item left: the estimation-error trend note in Cost calibration (watch as it passes 10 blocks).

## Reconciliation note (2026-07-04)
- On resuming this session it was detected that another session/machine had advanced the repo-trust backlog under its own numbering (preflight, resolve, deliverables, tests, push, release v0.1.0+SBOM) without reflecting it here. B4a-B6 were moved to Completed above, referencing the actual repo-trust repo commits. repo-trust's CLAUDE.md was also resynced, since it had gone stale against this spec's §1.2 (v13) since its B0 — see repo-trust's SESSION_STATE.md, "Version sync" section. Commit 479fb2b, mentioned as a reference to an external import ("initium"), was not found; it doesn't exist in either repo's history — pending clarification from the user on whether it belongs to another repo.

## Minimal context to resume
- references/SPEC.md is v25 (2026-07-06). CLAUDE.md synced byte-for-byte on every bump.
- Planned backlog empty. B21-B25 (Fable-fee experiment) closed everything: "Fable lane" discarded with evidence (references/FABLE_FEE_FINDINGS.md, see Discarded in IMPROVEMENTS.md); portfolio allocator shipped at ~/.claude/portfolio/ (outside the repo).
- session-budget is now a personal skill at ~/.claude/skills/session-budget/ (SKILL.md + SPEC.md v25 copied byte-for-byte) — any new project triggers it without copy/pasting the kickoff.
- Calibration (see "## Cost calibration" above, single source): S/Sonnet=4 (n=10, own). S/Fable and M/Sonnet with n=2 each, 1 more own actual needed to leave fallback. M/Fable and L have no data.
- Spec v25 fixed the root cause of an error flag (est should use the calibrated median, not the fixed default) — apply from the next new block on.
- allowances.json: ultrareview 1/3, resets 2026-08-01 — check if still current. fable_weekly still unpopulated (null).
- ~/.claude/portfolio/projects.json seeded with session-budget (prio 3) and repo-trust (prio 2), deadlines null — adjust if the allocator is used for real.
- Reports use the §5.1 templates (schematic checkpoint/close/resume with emoji).
